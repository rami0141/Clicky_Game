import React from "react";
import "./Header.css";

const Header = props => (
  <header className="text-center">
    <h1>Clicky Game</h1>
    <h6>Click on an image to earn points. Please do not click on an image more than once!</h6>
  </header>
);

export default Header;
